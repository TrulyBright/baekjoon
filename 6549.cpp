#include <cstdio>
#include <array>

using namespace std;

array<int, 100000> height;

int area(int start, int end) {

}
int main(void) {
  do {
    int n;
    scanf("%d", &n);
    for (int i=0; i<n; ++i) scanf("%d", &array[i]);
    
  } while(n!=0);
}
