#include <iostream>
#include <queue>
#include <vector>
using namespace std;

int main(void) {
  int T;
  cin >> T;
  while (T--) {
    int N, M;
    cin >> N, M;
    vector<int> priority;
    for (int i=0; i<N; i++) {
      int input; cin >> input; priority.push_back(input);
    }
    
  }
}
